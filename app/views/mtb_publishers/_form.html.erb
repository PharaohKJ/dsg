<%= form_for(@mtb_publisher) do |f| %>
  <% if @mtb_publisher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mtb_publisher.errors.count, "error") %> prohibited this mtb_publisher from being saved:</h2>

      <ul>
      <% @mtb_publisher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
