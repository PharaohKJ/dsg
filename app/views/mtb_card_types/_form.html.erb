<%= form_for(@mtb_card_type) do |f| %>
  <% if @mtb_card_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mtb_card_type.errors.count, "error") %> prohibited this mtb_card_type from being saved:</h2>

      <ul>
      <% @mtb_card_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
